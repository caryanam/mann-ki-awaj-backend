package com.mka.enums.translation;

/**
 * Enumeration representing the types of entities that can be translated in the system.
 */
public enum EntityType {
    POST,
    COMMENT,
    CHAT_MESSAGE,
    NOTIFICATION,
    PROFILE,
    OTHER
}
